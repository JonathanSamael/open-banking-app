name: open_banking_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.1
  http: ^1.5.0
  flutter_dotenv: ^6.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.7.1
  json_serializable: ^6.10.0

flutter:
  uses-material-design: true
  
  assets:
    - .env
    - assets
